<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.htjy.dao.StudentPayLogDao">
    
    <resultMap type="com.htjy.dbo.StudentPayLog" id="studentPayMap">
		<id column="id" property="id" />
		<result column="studentId" property="studentId" />
		<result column="project" property="project" />
		<result column="cost" property="cost" />
		<result column="payTime" property="payTime" />
		<result column="complete" property="complete" />
		<result column="refund" property="refund" />
	</resultMap>
	
	<insert id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
	    INSERT INTO
	    	t_student_pay_log (project,cost,payTime,studentId,complete,refund)
		values
			(#{project}, #{cost}, #{payTime}, #{studentId}, #{complete}, #{refund})
	</insert>
	
	<select id="find" resultMap="studentPayMap">
	    SELECT  
	    	id, project, cost, payTime, studentId, complete, refund
	    FROM t_student_pay_log
	    WHERE 1 = 1
	    AND	 id = #{id}
	</select>
    
	<update id="update">
	    UPDATE
	    	t_student_pay_log
	    SET 
	    	project = #{project}, cost = #{cost}, payTime = #{payTime},
	    	studentId = #{studentId}, complete = #{complete}, refund = #{refund}
	    WHERE
	   		id = #{id}
	</update>
	
</mapper>